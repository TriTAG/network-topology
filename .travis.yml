language: python
python:
  - "2.7"
addons:
  apt:
    packages:
      - libgeos-dev
      - libspatialindex-dev
install:
  - pip install .
script:
  - nosetests --with-cover --cover-package network_topology
